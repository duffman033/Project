<form method="post" action="{{ path('environnement_delete', {'id': environnement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ environnement.id) }}">
    <button class="btn">Delete</button>
</form>
