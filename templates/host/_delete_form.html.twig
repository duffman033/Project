<form method="post" action="{{ path('host_delete', {'id': host.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ host.id) }}">
    <button class="btn">Delete</button>
</form>
